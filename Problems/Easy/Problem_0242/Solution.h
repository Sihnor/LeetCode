#include <string>

class Solution {
private:
  /* data */
public:
  Solution(/* args */);
  ~Solution();

  bool isAnagram(std::string s, std::string t);
  bool isAnagram_VERSION1(std::string s, std::string t);
};
